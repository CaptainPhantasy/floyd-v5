Apply a standard unified diff/patch to the repository.
Use this when you want to modify multiple files at once or make complex structural changes.
The `patch` parameter must be a valid unified diff (e.g. `--- a/file.go \n +++ b/file.go`).
