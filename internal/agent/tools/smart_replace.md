Surgical search and replace. Replaces a unique block of text with new content.
Use this when you want to modify a specific section of a file without rewriting the entire file.

IMPORTANT: The `search` block must be unique enough to only match once in the file, and should match whitespace exactly.
