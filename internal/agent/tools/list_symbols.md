Extracts and lists high-level structural symbols (functions, classes, interfaces, types) from a file.
Use this to understand the API and structure of a file without spending tokens reading the entire content.
Returns the line numbers and signatures of top-level structures.
