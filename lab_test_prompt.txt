Perform your mandatory Core Initialization and output your Boot Summary.

Once initialized, I need you to verify all assumptions about the floyd-lab sandbox before we test the v5.0.2 SOTA patch.

## VERIFICATION STEPS (execute sequentially):

1. **Check MCP tool availability**: Verify that the `floyd-lab` MCP tools (`spawn_lab`, `execute_in_lab`, `migrate_to_host`, `teardown_lab`) are available in your tool list.

2. **Spawn a test lab**: Use `mcp_floyd-lab_spawn_lab` with `{"image":"floyd-lab:full","session_id":"sota-patch-test"}` to create a lab instance. Capture the output URL and confirm `/workspace` exists.

3. **Verify lab environment**: Inside the spawned lab, run verification commands:
   - Check if Go is installed (`which go` or `go version`)
   - Check Python3 version (`python3 --version`)
   - Check if `/workspace` contains a copy of the Floyd project
   - Check write permissions in `/workspace`

4. **Report findings**: Output a clear verification table with:
   - [ ] MCP tools available
   - [ ] Lab spawned successfully
   - [ ] `/workspace` exists
   - [ ] Go installed (or installable via `apt`)
   - [ ] Python3 ≥ 3.8
   - [ ] Project accessible in `/workspace`

5. **If any verification fails**, stop and report the exact issue before proceeding.

6. **If all verifications pass**, proceed to the next phase (patch testing) ONLY after getting my explicit go-ahead.

## IMPORTANT
- Be concise and output results in a structured format.
- Use the actual tool calls as shown in the session exports.
- Do NOT proceed to patch testing until I confirm.